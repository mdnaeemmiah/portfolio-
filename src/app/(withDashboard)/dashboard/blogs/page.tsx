import BlogTable from '@/components/form/BlogTable';
import React from 'react';

const page = () => {
    return (
        <div>
            <BlogTable></BlogTable>
        </div>
    );
};

export default page;