// import type { Metadata } from "next";
// import "./globals.css";
// import Providers from "@/providers";
// import { Toaster } from "react-hot-toast";
// // import Navbar from "@/components/shared/Navbar";
// // import { getServerSession } from "next-auth";
// // import Footer from "@/components/shared/Footer";

// export const metadata: Metadata = {
//   title: "Next Auth",
//   description: "Generated by create next app",
// };

// export default async function RootLayout({
//   children,
// }: Readonly<{
//   children: React.ReactNode;
// }>) {

//   // const session = await getServerSession();

//   return (
//     <html lang="en" data-theme="light">
//       <body cz-shortcut-listen="true">
//         {/* <Navbar  session={session}/> */}
//         <Providers>
//         <div className="min-h-screen" suppressHydrationWarning>{children}</div>
//         <Toaster position="top-center" reverseOrder={false} />
//         </Providers>
//         {/* <Footer></Footer> */}
//       </body>
//     </html>
//   );
// }







import type { Metadata } from "next";
import "./globals.css";
import Providers from "@/providers";
import { Toaster } from "react-hot-toast";

export const metadata: Metadata = {
  title: "Next Auth",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark"> {/* Change to 'light' or 'dark' as needed, or toggle dynamically */}
      <body className="bg-white text-black dark:bg-black dark:text-white transition-colors duration-300" suppressHydrationWarning>
        <Providers>
          <div className="min-h-screen">{children}</div>
          <Toaster position="top-center" reverseOrder={false} />
        </Providers>
      </body>
    </html>
  );
}
